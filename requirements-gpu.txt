# Windows/Ubuntu NVIDIA CUDA install only.
# Do not use this file on macOS.
--index-url https://download.pytorch.org/whl/cu126
--extra-index-url https://pypi.org/simple
-r requirements.txt
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1

# PaddleOCR uses the official onnxruntime engine in requirements.txt, so this
# file only replaces the PyTorch wheels with CUDA builds.
